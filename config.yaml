# Finnie configuration
app:
  name: "AI Finance Assistant"
  version: "0.0.1"

llm:
  model: "gpt-4o"
  embedding_model: "text-embedding-3-small"

rag:
  collection_name: "finnie_kb"
  persist_dir: "./chroma_db"
  chunk_size: 1000
  chunk_overlap: 200
  top_k: 5
